<?xml version="1.0" encoding="utf-8"?>
<!-- Track / buffered / played layers of the transport seek bar.
     SponsorBlock segments are painted on top of this by PlaybackSeekBar.onDraw(). -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:id="@android:id/background">
        <shape android:shape="rectangle">
            <size android:height="3dp" />
            <corners android:radius="2dp" />
            <solid android:color="@color/playback_seek_track" />
        </shape>
    </item>
    <item android:id="@android:id/secondaryProgress">
        <clip>
            <shape android:shape="rectangle">
                <size android:height="3dp" />
                <corners android:radius="2dp" />
                <solid android:color="@color/playback_seek_buffer" />
            </shape>
        </clip>
    </item>
    <item android:id="@android:id/progress">
        <clip>
            <shape android:shape="rectangle">
                <size android:height="3dp" />
                <corners android:radius="2dp" />
                <solid android:color="@color/playback_accent" />
            </shape>
        </clip>
    </item>
</layer-list>
